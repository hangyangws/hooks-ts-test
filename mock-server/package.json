{
  "name": "mock-server",
  "description": "A mock server for homework",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
